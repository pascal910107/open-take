{
  "name": "@open-take/editor",
  "version": "0.0.0",
  "private": true,
  "description": "Editor v4 \u2014 icon-rail layered inspector + timeline over the live cinematic preview. Refines composition.json; never touches the captured video.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@fontsource-variable/instrument-sans": "^5.2.8",
    "@fontsource/geist-mono": "^5.2.8",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.4.0",
    "typescript": "^5.6.3",
    "vite": "^6.3.0"
  }
}
