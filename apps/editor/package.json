{
  "name": "@open-take/editor",
  "version": "0.0.0",
  "private": true,
  "description": "Web editor for the cinematic layer — live WYSIWYG preview of a TakeComposition over the capture video (read-only milestone). Refines composition.json; never touches the captured video.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.8",
    "@fontsource/jetbrains-mono": "^5.2.8",
    "cmdk": "^1.1.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.4.0",
    "typescript": "^5.6.3",
    "vite": "^6.3.0"
  }
}
