{
  "name": "@open-take/cli",
  "version": "0.0.0",
  "description": "open-take CLI — inspect an app and render an agent-planned demo.",
  "license": "MIT",
  "type": "module",
  "bin": { "open-take": "./dist/cli.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@open-take/runtime": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "tsdown": "^0.9.0",
    "typescript": "^5.6.3"
  }
}
