{
  "name": "@open-take/adapter-agent-browser",
  "version": "0.0.0",
  "description": "agent-browser BrowserDriver. v1 PRIMARY browser adapter (D8).",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "test": "node --test --import tsx/esm test/*.test.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@open-take/core": "workspace:*"
  },
  "peerDependencies": {
    "agent-browser": "^0.27.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "agent-browser": "^0.27.0",
    "tsdown": "^0.9.0",
    "tsx": "^4.22.3",
    "typescript": "^5.6.3"
  }
}
