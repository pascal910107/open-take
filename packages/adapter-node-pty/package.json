{
  "name": "@open-take/adapter-node-pty",
  "version": "0.0.0",
  "private": true,
  "description": "Dormant node-pty + asciinema capture retained for future terminal recording.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "node-pty": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "tsdown": "^0.9.0",
    "typescript": "^5.6.3"
  }
}
