node_modules
dist
out
.turbo
.open-take-cache
*.log
.DS_Store
.env
.env.local
.claude/
.notes/

# throwaway spikes — kept local (like .notes/spike-polish)
/spike-revideo/

# rendered artifacts — regenerable, bloaty
*.mp4
*.png
*.webm

# built skill bundle (copied from skills/open-take at build)
packages/cli/skill/

# README copied from the repo root at build (npm needs it inside the package)
packages/cli/README.md

# bundled editor SPA (copied from apps/editor/dist at prepack)
packages/runtime/editor-dist/
